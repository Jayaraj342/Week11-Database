server:
  port: 8889
spring:
  application.name: ADMIN-SERVICE
  jpa.hibernate.ddl-auto: none
  datasource.initialization-mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/greatlearning
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: pluralsight
